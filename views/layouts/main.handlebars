<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Talkin' Movies</title>
        <link rel="stylesheet" href="/stylesheets/style.css">
    </head>

    <body>
        <div class="wrapper">
            <header>
                <div>
                    <nav>
                        <a href="/">Home</a>
                        <a href="/dashboard">Profile</a>
                        {{#if loggedIn}}
                        <a href="/" id="logout">Logout</a>
                        {{else}}
                        <a href="/login">Login</a>
                        {{/if}}
                    </nav>
                </div>
                <div>
                    <h1 class="title">Talkin' Pictures</h1>
                </div>
            </header>
            <main>
                {{{ body }}}
            </main>
        </div>
        {{#if loggedIn}}
        <script src="/javascript/logout.js"></script>
        {{/if}}
    </body>
</html>